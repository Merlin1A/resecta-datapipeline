# Allowlist for scripts/hygiene_gate.py (the planning-identifier gate).
# Row forms: `token <TOKEN>` or `text <path>:<substring>`; anything after two
# spaces and a `#` is the reason. Keep every row reasoned; the gate fails on
# any hit this file does not cover.

# Public tokens the register-id shape collides with: IRS form names.
token W-2  # IRS Form W-2 (Wage and Tax Statement): corpus templates, classifier seeds, context labels, scope rules
token W-9  # IRS Form W-9 (Request for Taxpayer Identification Number): corpus template, scope rules

# An emitted payload string pinned by tests/test_g8_bucket_recall.py and by the
# asset_hashes.lock row for demographics/g8_bucket_recall_v1.json; rewording it
# moves a lock row, which is a separate, hash-explained change.
text src/resecta_data/demographics/g8_bucket_recall.py:F-10 ack option a  # lock-pinned emitted note
